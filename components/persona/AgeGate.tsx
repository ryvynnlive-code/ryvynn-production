'use client';

// AgeGate removed.
// Showing "unfiltered trauma discussions" warnings as the first thing
// a distressed user sees raises anxiety instead of offering relief.
// Compliance is handled by the footer legal disclaimer on every page.
// The Guardian page has its own "not therapy" disclaimer inline.

export function AgeGate() {
  return null;
}
